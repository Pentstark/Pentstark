name: Build & Deploy to Hostinger

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣ Checkout code
    - name: Checkout repository
      uses: actions/checkout@v4

    # 2️⃣ Check disk space before starting
    - name: Check initial disk space
      run: df -h

    # 3️⃣ Setup Node.js
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    # 4️⃣ Clean npm cache
    - name: Clean npm cache
      run: npm cache clean --force

    # 5️⃣ Install dependencies (without caching to save disk space)
    - name: Install dependencies
      run: npm ci

    # 6️⃣ Build project
    - name: Build project
      run: npm run build

    # 7️⃣ Verify build output
    - name: Check dist folder exists
      run: |
        if [ ! -d "dist" ]; then
          echo "❌ Build failed: dist folder not found!"
          exit 1
        else
          echo "✅ Build folder exists."
        fi

    # 8️⃣ Check disk space after build
    - name: Check disk space post-build
      run: df -h

    # 9️⃣ Setup SSH agent
    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.9.0
      with:
        ssh-private-key: ${{ secrets.HOSTINGER_SSH_KEY }}

    # 🔟 Prepare Hostinger deployment (clear & create folders)
    - name: Prepare Hostinger deployment
      env:
        DEPLOY_PATH: /home/u452452192/domains/safeproduction.pentstark.com
      run: |
        ssh -p 65002 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null u452452192@147.93.109.200 "set -e && \
          echo '🚀 Starting deployment...' && \
          rm -rf $DEPLOY_PATH/* && \
          mkdir -p $DEPLOY_PATH/public_html"

    # 1️⃣1️⃣ Upload dist folder
    - name: Upload dist folder
      env:
        DEPLOY_PATH: /home/u452452192/domains/safeproduction.pentstark.com
      run: |
        echo "📦 Uploading dist folder..."
        rsync -avz --delete -e "ssh -p 65002 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null" dist/ u452452192@147.93.109.200:$DEPLOY_PATH/public_html/

    # 1️⃣2️⃣ Deploy remaining code to public_html/main
    - name: Deploy remaining code to public_html/main
      env:
        DEPLOY_PATH: /home/u452452192/domains/safeproduction.pentstark.com
      run: |
        echo "📂 Deploying remaining files..."
        mkdir temp_deploy
        rsync -av --exclude='dist' ./ temp_deploy/

        ssh -p 65002 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null u452452192@147.93.109.200 "mkdir -p $DEPLOY_PATH/public_html/main"

        rsync -avz -e "ssh -p 65002 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null" temp_deploy/ u452452192@147.93.109.200:$DEPLOY_PATH/public_html/main/

    # 1️⃣3️⃣ Cleanup temporary folder and dist
    - name: Cleanup temporary and build folders
      run: |
        echo "🧹 Cleaning temp folders..."
        rm -rf temp_deploy dist

    # 1️⃣4️⃣ Final disk space check
    - name: Final disk space check
      run: df -h

    # 1️⃣5️⃣ Final deployment message
    - name: Deployment complete
      run: echo "✅ Deployment finished successfully!"
